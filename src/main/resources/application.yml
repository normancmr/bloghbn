Spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=EST
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create